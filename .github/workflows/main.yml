name: QA-CI/CD
on:
  workflow_dispatch:
env:
  BuildNumber: ${{github.run_id}}-${{github.run_number}}
jobs:
  setup_and_compile:
    container:
      image: node:18
    runs-on: ubuntu-latest
    
    steps:
      - name: checking
        shell: bash
        run: echo "${{github.run_number}}"
